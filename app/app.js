(function() {
    var app = angular.module('app', ['conteudos', 'pessoas', 'projetos', 'assistentes']);
    app.controller('appController', ['$scope', '$http',
        function($scope, $http) {
            
        }
    ]);
})();